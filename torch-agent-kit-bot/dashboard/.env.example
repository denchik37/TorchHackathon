# Optional: Bot account for on-chain data (Account page + Overview card).
# No private key needed — dashboard only reads from Hedera Mirror Node.
BOT_ACCOUNT_ID=0.0.XXXXXXXX
BOT_NETWORK=mainnet

# Option A (Vercel): Hetzner read-only API. Token must NOT be NEXT_PUBLIC_ (server-side only).
TORCH_API_BASE=http://65.108.254.251:3001
DASHBOARD_API_TOKEN=your_secret_token
