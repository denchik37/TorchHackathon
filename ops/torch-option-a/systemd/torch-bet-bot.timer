# Template: copy to /etc/systemd/system/torch-bet-bot.timer
# Runs torch-bet-bot.service daily (e.g. 12:00 UTC).

[Unit]
Description=Run Torch bet bot daily

[Timer]
OnCalendar=*-*-* 12:00:00 UTC
Persistent=yes

[Install]
WantedBy=timers.target
