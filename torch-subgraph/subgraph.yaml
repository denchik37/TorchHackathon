specVersion: 0.0.4
description: Torch prediction market subgraph
repository: https://github.com/hashgraph/hedera-subgraph-example
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum/contract
    name: TorchPredictionMarket
    network: mainnet
    source:
      address: "0x15015770b8d5177fb642e58f5de7f3b01040c9f0"
      abi: TorchPredictionMarket
      startBlock: 87248805
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.6
      language: wasm/assemblyscript
      entities:
        - User
        - UserStats
        - Bet
        - Fee
        - Bucket
      abis:
        - name: TorchPredictionMarket
          file: ./abis/TorchPredictionMarket.json
      eventHandlers:
        - event: BetPlaced(indexed uint256,indexed address,uint256,uint256,uint256,uint256,uint256)
          handler: handleBetPlaced
        - event: BetFinalized(indexed uint256,uint256,bool,uint256)
          handler: handleBetFinalized
        - event: BetClaimed(indexed uint256,indexed address,uint256)
          handler: handleBetClaimed
        - event: FeeCollected(uint256)
          handler: handleFeeCollected
        - event: AggregationCompleted(indexed uint256,uint256)
          handler: handleAggregationCompleted
        - event: BucketPriceSet(indexed uint256,uint256)
          handler: handleBucketPriceSet
      file: ./src/mappings.ts
